bin.name = "split_rolls"
args = "invalid-version.zip"
status = "failed"
fs.sandbox = true
fs.base = "../data"

stderr = """
[..] [ERROR] Invalid metadata: Malformed version for JSON export, expected '1.0' got '1.1'
"""
